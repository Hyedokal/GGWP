name: CI for search-service

on:
  push:
    branches:
      - deploy#*
    paths:
      - 'Search-service/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # workflows Settings (워크플로우에서 작업할 소스 코드 환경 복사)
      - name: Checkout Repository
        uses: actions/checkout@v3

        # Settings
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Copy Secret
        env:
          BOOTSTRAP_YML_URL: ${{ secrets.BOOTSTRAP_URL }}
          BOOTSTRAP_YML_SEARCH: ${{ secrets.BOOTSTRAP_SEARCH }}
          SECRET_DIR: ./Search-service/src/main/resources
          SECRET_FILENAME: bootstrap.yml
        run: |
          echo $BOOTSTRAP_YML_URL > $SECRET_DIR/$SECRET_FILENAME
          echo $BOOTSTRAP_YML_SEARCH >> $SECRET_DIR/$SECRET_FILENAME

        # Gradle Settings
      - name: Grant execute permission for gradlew
        run: chmod +x Search-service/gradlew

        # Gradle Build
      - name: Build with Gradle
        working-directory: Search-service
        run: ./gradlew clean build -x test

        # Docker Build
      - name: Build Docker image
        working-directory: Search-service
        run: docker build -t hyedokal/ggwp_search-service .

        # DockerHub Login
      - name: Log in to Docker Hub
        run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_PASSWORD }}

        # DockerHub push
      - name: Push Docker image
        run: docker push hyedokal/ggwp_search-service